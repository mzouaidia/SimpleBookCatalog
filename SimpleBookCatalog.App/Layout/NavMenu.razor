<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Prosty Katalog Książek</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill" aria-hidden="true"></span> Start
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="books">
                <span class="bi bi-table" aria-hidden="true"></span> Książki (table html)
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="books-quick-grid">
                <span class="bi bi-collection-fill" aria-hidden="true"></span> Książki (quick grid)
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="books-syncfusion-grid">
                <span class="bi bi-columns-gap" aria-hidden="true"></span> Książki (syncfusion)
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="books-radzen-grid">
                <span class="bi bi-layout-text-window-reverse" aria-hidden="true"></span> Książki (radzen grid)
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

}